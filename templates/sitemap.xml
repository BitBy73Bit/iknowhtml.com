<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    {# Static pages #}
    <url>
        <loc>{{ url() }}</loc>
        <lastmod>{{ now|date("c") }}</lastmod>
    </url>

    <url>
        <loc>{{ url('why') }}</loc>
        <lastmod>{{ now|date("c") }}</lastmod>
    </url>

    <url>
        <loc>{{ url('leaderboard') }}</loc>
        <lastmod>{{ now|date("c") }}</lastmod>
    </url>

    <url>
        <loc>{{ url('submit') }}</loc>
        <lastmod>{{ now|date("c") }}</lastmod>
    </url>

    {# All submissions that are "live" #}
    {% set entries = craft.entries.section('submissions').status("live").uri("not ''").all() %}
    {% for entry in entries %}
    <url>
        <loc>{{ entry.url }}</loc>
        <lastmod>{{ entry.dateUpdated|date("c") }}</lastmod>
    </url>
    {% endfor %}

</urlset>